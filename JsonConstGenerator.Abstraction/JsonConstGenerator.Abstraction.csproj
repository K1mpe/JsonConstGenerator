<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
	  <LangVersion>latest</LangVersion>

    <!-- Not published on its own: its output is bundled into the JsonConstGenerator
         package (see JsonConstGenerator.csproj) so consumers only install one package. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

</Project>
